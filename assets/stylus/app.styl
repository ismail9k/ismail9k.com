@import 'config/_index';
@import 'typography';
@import 'theme';
@import 'spacing';

* {
  box-sizing: border-box;
}

.sr-only {
  position: absolute;
  overflow: hidden;
  clip: rect(0, 0, 0, 0);
  margin: -1px;
  padding: 0;
  width: 1px;
  height: 1px;
  border: 0;
}
